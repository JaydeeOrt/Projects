@model GrumpyCoastGames.Models.Game

<h2>Edit Existing Game</h2>

<form asp-action="EditGame" method="post" enctype="multipart/form-data">
    <input type="hidden" asp-for="ID" />
    <div class="container border border-danger round-2 p-3">
        <div class="mb-3">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Description" class="form-label"></label>
            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Stage" class="form-label"></label>
            <input asp-for="Stage" class="form-control" />
            <span asp-validation-for="Stage" class="text-danger"></span>
        </div>
        @if (!string.IsNullOrEmpty(Model.Image))
            {
            <div class="mb-3">
                <label class="form-label">Current Image</label>
                <img src="@Model.Image" height="100" />
            </div>
            }
        <div class="mb-3">
            <label class="form-label">Upload Image</label>
            <input type="file" name="imageFile" class="form-control" accept="image/*" />
        </div>
    </div>
    <div class="p-2"> </div>
    <button type="submit" class="btn btn-primary">Save Game</button>
    <a asp-action="Games" class="btn btn-secondary">Cancel</a>
</form>